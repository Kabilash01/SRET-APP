name: sret_app
description: "SRET Faculty Portal - Sri Ramachandra Faculty of Engineering & Technology"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  go_router: ^14.0.0
  google_fonts: ^6.2.1
  google_sign_in: ^6.2.1
  shared_preferences: ^2.2.2
  flutter_animate: ^4.5.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
